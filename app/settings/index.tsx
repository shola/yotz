/**
 * Starter route that matches '/settings'
 * 
 * Routes are a a collection of reuseable components and should
 * import and instantiate components, components should not live in
 * routes. There should be a parallel directory structure for
 * components.
 */ 