/**
 * This is a shared route. It will create 'app/game/[user.tsx]' and 
 * 'app/history/[user].tsx' routes instead of creating them of having
 * to duplicate the [user].tsx route in multiple directories.
 */ 